type Point = {
  x: number;
  y: number;
};

type Size = {
  w: number;
  h: number;
};

type Keys =
  | "ArrowUp"
  | "ArrowDown"
  | "ArrowLeft"
  | "ArrowRight"
  | "Space"
  | "Escape"
  | "KeyP";
